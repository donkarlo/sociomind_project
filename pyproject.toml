[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "aix"
version = "1.0.0"

[project.scripts]
boot = "sociomind.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
